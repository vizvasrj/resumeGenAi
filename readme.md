run docker

```bash
docker run --publish=8000:8000 spawnia/md-to-pd 
```

to run backend

```bash
cd backend
go run cmd/main.go
```

install frontend in firefox browser
[about:debugging](about:debugging)
